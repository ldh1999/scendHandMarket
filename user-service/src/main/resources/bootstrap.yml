spring:
  profiles:
    active: prod
  application:
    name: userservice
  cloud:
    nacos:
      discovery:
        cluster-name: TJ
      #server-addr: 8848
      config:
        file-extension: yaml
        server-addr: 101.201.121.55:8848