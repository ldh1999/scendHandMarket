spring:
  profiles:
    active: dev
  application:
    name: otherResourceservice
  cloud:
    nacos:
      discovery:
        cluster-name: TJ
      #server-addr: 8848
      config:
        file-extension: yaml
