spring:
  application:
    name: orderservice
  profiles:
    active: dev
  cloud:
    nacos:
      discovery:
        cluster-name: TJ
      #server-addr: 8848
      config:
        file-extension: yaml
